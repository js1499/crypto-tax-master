# Glide — conversion landing, variant B "Harbor"

Same anatomy and reviewed copy as `apps/glide-landing`, reskinned: deep teal + terracotta CTA on
warm sand, Plus Jakarta Sans, rounded/soft. Generated from the base via `build-variant.mjs`
(style/fonts/favicon/brand-hex swap only — copy is byte-identical). Serve:
`python -m http.server 8914 -d apps/glide-landing-b` or `serve.mjs --dir apps/glide-landing-b --port 8924`.
